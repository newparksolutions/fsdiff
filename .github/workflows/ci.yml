name: CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  build-and-test:
    name: ${{ matrix.os }} - ${{ matrix.compiler }} - ${{ matrix.simd }}
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          # Ubuntu x64 GCC with SIMD
          - os: ubuntu-x64
            runner: ubuntu-latest
            compiler: gcc
            simd: with-simd
            cmake_flags: ""
            cc: gcc
            cxx: g++

          # Ubuntu x64 GCC without SIMD
          - os: ubuntu-x64
            runner: ubuntu-latest
            compiler: gcc
            simd: no-simd
            cmake_flags: "-DFSDIFF_ENABLE_SIMD=OFF"
            cc: gcc
            cxx: g++

          # Ubuntu x64 Clang with SIMD
          - os: ubuntu-x64
            runner: ubuntu-latest
            compiler: clang
            simd: with-simd
            cmake_flags: ""
            cc: clang
            cxx: clang++

          # Ubuntu x64 Clang without SIMD
          - os: ubuntu-x64
            runner: ubuntu-latest
            compiler: clang
            simd: no-simd
            cmake_flags: "-DFSDIFF_ENABLE_SIMD=OFF"
            cc: clang
            cxx: clang++

          # ARM with SIMD
          - os: arm
            runner: ARM_Runner
            compiler: gcc
            simd: with-simd
            cmake_flags: ""
            cc: gcc
            cxx: g++

          # ARM without SIMD
          - os: arm
            runner: ARM_Runner
            compiler: gcc
            simd: no-simd
            cmake_flags: "-DFSDIFF_ENABLE_SIMD=OFF"
            cc: gcc
            cxx: g++

          # Windows x64 with SIMD
          - os: windows-x64
            runner: windows-latest
            compiler: msvc
            simd: with-simd
            cmake_flags: ""

          # Windows x64 without SIMD
          - os: windows-x64
            runner: windows-latest
            compiler: msvc
            simd: no-simd
            cmake_flags: "-DFSDIFF_ENABLE_SIMD=OFF"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install build tools (Linux)
        if: runner.os != 'Windows'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake clang

      - name: Configure CMake
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: |
          cmake -B build -S . ${{ matrix.cmake_flags }}

      - name: Build
        run: |
          cmake --build build --config Release --parallel

      - name: Run tests
        run: |
          cd build
          ctest --output-on-failure -C Release

      - name: Display build info
        shell: bash
        env:
          CC: ${{ matrix.cc }}
        run: |
          if [ "$RUNNER_OS" != "Windows" ]; then
            echo "Compiler: $($CC --version | head -1)"
          fi
          cd build
          if [ "$RUNNER_OS" = "Windows" ]; then
            ./Release/fsdiff.exe --version || ./bin/Release/fsdiff.exe --version || echo "fsdiff binary not found"
          else
            ./bin/fsdiff --version
          fi
          echo "All tests completed successfully"
